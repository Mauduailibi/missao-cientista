{
  "name": "missao-cientista",
  "private": true,
  "packageManager": "pnpm@11.21.0",
  "scripts": {
    "dev": "pnpm --filter frontend dev",
    "build": "pnpm --filter frontend build && rm -rf dist && cp -R frontend/dist dist",
    "lint": "pnpm --filter frontend lint",
    "preview": "pnpm --filter frontend preview"
  },
  "dependencies": {
    "hono": "^4.13.5"
  }
}
